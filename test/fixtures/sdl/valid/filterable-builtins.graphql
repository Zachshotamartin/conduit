type Query {
  ok: Boolean!
}

type Subscription {
  events(
    text: String @filterable
    id: ID @filterable
    count: Int @filterable
    ratio: Float @filterable
    present: Boolean @filterable
  ): String
}
