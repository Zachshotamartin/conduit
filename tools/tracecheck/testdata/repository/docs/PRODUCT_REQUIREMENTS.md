# Fixture Product Requirements

## 6. Not Requirements

`FR-OUTSIDE-998` is a decoy outside the owned sections.

## 7. Functional Requirements

- `FR-OPS-002`: Configuration is validated.

## 8. Failure UX

`FR-OUTSIDE-999` is another decoy.

## 9. Non-Functional Requirements

- `NFR-MAINT-005`: Runtime dependencies have an explicit budget.

## 10. Acceptance

`NFR-OUTSIDE-999` is not a requirement definition.
